import { Meta, StoryObj } from "@storybook/react";
import { Fallback } from ".";

export default {
  title: "Components/Fallback",
  component: Fallback,
} as Meta<typeof Fallback>;

export const Default: StoryObj<typeof Fallback> = {};
